// Dónde vive la API que consume este sitio. Vacío porque el frontend y el backend
// se sirven desde el mismo dominio - si algún día se separan, acá va la URL del
// backend (ej: 'https://api.tudominio.com').
window.API_BASE = '';
